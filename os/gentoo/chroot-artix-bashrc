# vim: set ft=sh

mountpoint -q /boot/efi || sudo mount /boot/efi

mountpoint -q ~/depot || sudo mount ~/depot
mountpoint -q ~/storage || sudo mount ~/storage

# could lead to unexpected results
# sudo mkdir /run/openrc
# sudo touch /run/openrc/softlevel
# sudo /etc/init.d/name start

. ~/.bashrc

if [[ -x "$HOME/.config/xsettingsd/set-color-scheme" ]]; then
    "$HOME/.config/xsettingsd/set-color-scheme" "$COLOR_SCHEME"
fi
